we should display the image version for container runner in the `gh sr status`

---

Implement the plan as specified, it is attached for your reference. Do NOT edit the plan file itself.

To-do's from the plan have already been created. Do not create them again. Mark them as in_progress as you work, starting with the first one. Don't stop until you have completed all the to-dos.

---

Implement the plan as specified, it is attached for your reference. Do NOT edit the plan file itself.

To-do's from the plan have already been created. Do not create them again. Mark them as in_progress as you work, starting with the first one. Don't stop until you have completed all the to-dos.

---

fix the confict in git pull rebase. Finish the rebase

---

The container version is `gh-sr/agentic-runner:2.334.0`, not reflecting the version of gh-sr. After update the image code, I need to `gh sr rebuild` and I want to check if they're running the latest image.

---

Implement the plan as specified, it is attached for your reference. Do NOT edit the plan file itself.

To-do's from the plan have already been created. Do not create them again. Mark them as in_progress as you work, starting with the first one. Don't stop until you have completed all the to-dos.

---

Implement the plan as specified, it is attached for your reference. Do NOT edit the plan file itself.

To-do's from the plan have already been created. Do not create them again. Mark them as in_progress as you work, starting with the first one. Don't stop until you have completed all the to-dos.

---

I ran `gh sr rebuild --repo=an-lee/rune` and then check the `gh sr`, the runners of rune display the image version. Then I ran `gh sr rebuild --repo=an-lee/enjoy_web`, in `gh sr`, the version of rune runners are gone, just `?` like others, but the `enjoy_web` runners display ther image version.

Why is that? Is there something wrong?

---

I ran `gh sr rebuild --repo=an-lee/rune` and then check the `gh sr`, the runners of rune display the image version. Then I ran `gh sr rebuild --repo=an-lee/enjoy_web`, in `gh sr`, the version of rune runners are gone, just `?` like others, but the `enjoy_web` runners display ther image version.

Why is that? Is there something wrong?