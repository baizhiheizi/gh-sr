---
name: run-2026-07-14-29318676288
description: Run history entry for Repo Assist run 29318676288 (selected tasks 3, 10, 5)
metadata:
  type: project
---

# Run 29318676288 — 2026-07-14 09:25 UTC

## Selected: 3, 10, 5. Tasks 5 + 10 done (two PRs); Task 3 fallback.

## Task 3 — fallback (no candidate)
0 open issues labelled `bug` / `help wanted` / `good first issue`. #359 + #360 are code-quality duplicate-code issues addressed by Task 5.

## Task 5 — `repo-assist/improve-benchstat-formatdelta-and-buffer-2026-07-14` @ `73ba243`
`scripts/benchstat/benchstat.go` +23 / −14, `benchstat_test.go` −17. Closes #360, partial fix for #359.

- Removes dead public `FormatDelta` (only caller was `TestFormatDelta`, also deleted).
- Replaces `FormatDelta` body with `appendDelta(sb, d)` helper that owns the 24-byte stack buffer used by `writeMetricCell`. Inline `[24]byte + formatDeltaTo` copy in `writeMetricCell` collapses to a one-line call.
- Extracts local `appendFloat(dst, v, prec)` analogue of `internal/strfmt.FmtFloat`. benchstat is `//go:build ignore`, stdlib-only (see `.github/workflows/bench-compare.yml` and the package doc on `internal/strfmt/fmtstr.go`) so it cannot import `internal/strfmt`. Mirrors the 24-byte worst-case rule in one declaration site.
- Normalizes `writeNumber`'s `[32]byte` to `[24]byte` to match the documented worst case used by every other call site (`[32]byte` was over-provisioned).

Net: 7 lines of duplication removed, one declaration site for the 24-byte worst case, zero behaviour change. The format-delta rendering path is still covered by `TestRenderMarkdown_HeaderAndTable`, `TestRenderMarkdown_FailSummary`, `TestRenderMarkdown_WarnSummary`, `TestEndToEnd_RoundTripThroughMarkdown`. PR reported success. Recovery: `/tmp/gh-aw/aw-repo-assist-improve-benchstat-formatdelta-and-buffer-2026-07-14.{patch,bundle}` (5.7 KB / 2.2 KB).

## Task 10 — `repo-assist/test-doctor-has-container-agentic-runners-2026-07-14` @ `edc1bed`
`internal/doctor/doctor_test.go` +44. `TestHasContainerAgenticRunners` (5 sub-cases) pins the host-agnostic predicate gating `checkContainerAgenticInnerHygiene`. Cases: empty list → false, native-only → false, `runner_mode: container` without `profile: agentic` → false, agentic on a different host → true (helper is host-agnostic; caller `installTargetsForHost` does the per-host filter — a test comment names this contract), mixed list with at least one agentic → true.

Coverage impact:
- `hasContainerAgenticRunners`: 0.0% → 100.0%
- `internal/doctor` overall: 69.5% → **70.7%** (+1.2 pp)

The other two 0%-coverage helpers (`checkContainerRunnerInstall`, `checkContainerAgenticInnerHygiene`) need mock-seam refactors against `runner.ContainerBootstrapFailed` / `runner.ProbeDinDContainerReadiness` / `runner.ContainerStateStatus` / `agentic.ValidateAWFHygieneInner` / `agentic.ValidateContainerAgenticFanout` / `runner.DetectHostEgressMTU`. Non-trivial; deferred to a future run. PR reported success. Recovery: `/tmp/gh-aw/aw-repo-assist-test-doctor-has-container-agentic-runners-2026-07-14.{patch,bundle}` (3.9 KB / 2.0 KB).

## Task 11 — `update_issue` on #309
Body rebuilt: removed merged-#-PRs (#355/#354/#353/#352/#351/#349/#348/#344, all merged 2026-07-10/11), added this run's two new PRs. Future Work: removed `hasContainerAgenticRunners` from the doctor coverage list (now covered); remaining two helpers documented with the seam refactor needed. Run History entry prepended for 2026-07-14 09:25 UTC (run 29318676288). Older entries from 2026-07-08/07/04/03/01 collapsed to a pointer line because the issue body had grown past the 10 KB tool-input limit.

## Verified
- `go build ./...` OK
- `go vet ./...` clean
- `gofmt -l .` clean
- `go test ./... -count=1` 16/16 packages OK
- `go test ./... -race -count=1` no races
- `go test ./scripts/benchstat/ -count=1 -v` 19/19 PASS
- `go test ./internal/doctor/ -cover -count=1` 70.7% (was 69.5%)
- `BenchmarkRenderMarkdown` 3 reps: 5262/5863/5573 ns/op 2560 B/op 2 allocs/op — unchanged within noise vs. pre-PR baseline

## Discovered contracts (for future reference)
- **`hasContainerAgenticRunners` is host-agnostic** — it reports "does any runner (anywhere) use agentic container mode?". Per-host filtering happens at the caller `installTargetsForHost` (via `agentic := func(rc *config.RunnerConfig) bool { return rc.IsAgentic() }` predicate). A test comment names this explicitly so a future refactor that conflates "agentic on this host" with "any agentic" gets flagged.
- **`scripts/benchstat/fmtstr.go`-equivalent helper is intentionally local** — `internal/strfmt.FmtFloat` exists, but `scripts/benchstat` is `//go:build ignore` and stdlib-only (documented in the package doc on `internal/strfmt/fmtstr.go` and in `.github/workflows/bench-compare.yml`). The local `appendFloat` mirrors the 24-byte worst-case rule in one declaration site.
- **Buffer-size rationale lives in `appendFloat`'s doc** — the previous three call-site comments (`writeNumber`, `formatDeltaTo`, `FormatDelta`) each re-derived the 24-byte rule. The new doc consolidates the rationale and points at `internal/strfmt/fmtstr.go` for upstream context.

## Open-source posture
- Phantom-PR backlog remains resolved.
- Next Task 5 candidate: cross-package dedup waiting on maintainer pick from #341 (closed) or new cross-cutting refactor.
- Next Task 9 candidate: `internal/doctor/doctor.go` per-host check helpers `checkContainerRunnerInstall` + `checkContainerAgenticInnerHygiene` — need seam refactors first (see run notes above).
- Next Task 2 candidate: none (no candidate since #341 closed 2026-07-10; #132 on hold awaiting maintainer signal).
- Next Task 8 candidate: `internal/hostshell/ps/` `Exec`/`CombinedOutput` (cold-path, hard cross-platform) — small win, low priority.
- Next Task 10 candidate: open PR #362 (test-improver) is a draft with `[agentic-threat-detected]` label — needs maintainer decision before any follow-up.