---
name: run-2026-07-07-28878070395
description: Run history entry for Repo Assist run 28878070395 (selected tasks 9, 3, 2)
metadata:
  type: project
---

# Run 28878070395 — 2026-07-07 15:35 UTC

## Selected tasks
- Task 9, Task 3, Task 2
- Task 3 fallback applied (no bugs/help-wanted/good-first-issue). Tasks 9 and 2 executed substantively.

## Task 9 (Testing) — completed
- **Branch:** `repo-assist/test-host-metrics-collect-dispatch-2026-07-07` @ `9a63cb3`
- **File:** `internal/host/metrics_test.go`, +158
- **New tests:** `TestCollectMetrics_DispatchOS` (linux/darwin/windows dispatch sub-cases), `TestUnixMetricsScript_Branches` (linux + darwin + unknown-OS-falls-back-to-linux), `TestWindowsMetricsScript_Body` (CIM markers + sentinels), `TestCollectMetrics_ScriptErrorPropagates` (shell-error wrap path).
- **What was locked:** the per-OS script generator picked by `Host.CollectMetrics`. Windows test asserts the wrapper surface (`powershell.exe` + `-EncodedCommand`) because `host.wrapCommand` base64-encodes the script for non-local Windows addrs; the script body is locked separately by `TestWindowsMetricsScript_Body`.
- **create_pull_request** reported success. Patch + bundle at `/tmp/gh-aw/aw-repo-assist-test-host-metrics-collect-dispatch-2026-07-07.{patch,bundle}` (5.6 KB / 3.9 KB) as a recovery fallback.

## Task 3 (Fix) — fallback (no-op)
- 0 open issues labelled `bug` / `help wanted` / `good first issue`. Duplicate-code issues (#325, #326, #327) are real refactors but multi-hour. Task 2 engagement was the proportionate response this run.

## Task 2 (Comment) — two substantive comments
- **Issue #325** — `[duplicate-code] Duplicate Code: Agentic container probe metadata duplicated between per-check wrappers and fanout`. Flagged that `internal/agentic/agentic_fanout_test.go` snapshots both the assembled command and the spec map (so any unification has to update tests in lockstep), and offered a surgical sub-scope: inline the inlined shell bodies in `containerAgenticFanoutCheckCommand` (lines ~760-805) using the existing `*CheckCommand` helpers at lines 945-1005. ~45 lines removed, contained test surface change. Awaiting maintainer call.
- **Issue #307** — `[repo-assist] eng(ci): add bench-compare workflow (closes #124, benchstat piece)`. Confirmed `/tmp/gh-aw/aw-repo-assist-eng-bench-compare-2026-07-01.{patch,bundle}` are no longer present in the sandbox (wiped between runs). Three recovery paths offered: (1) `gh run download 28545571496 -n agent` from a maintainer workstation with repo scope — recommended since the artifact retention runs to late September 2026 (90 days from 2026-07-01), (2) re-create from the issue body, (3) ask Repo Assist to regenerate in a clean run.

## Task 11 (mandatory) — `update_issue` on #309
- New run entry prepended to Run History. Suggested Actions rebuilt: review PRs (#328 perf-improver, #323 efficiency-improver, the new test PR), check comments (#325 new, #307 new, #326, #312, #313, #124), apply patch (#307 bench-compare). Future Work section expanded to include the surgical sub-scope for #325 and the doctor per-host check helpers follow-up.

## Verified
- `go build ./...` OK
- `go vet ./...` clean
- `gofmt -l .` clean
- `go test ./internal/host/ -race -count=1` all host tests pass (4 new tests, 8 new sub-tests)
- `go test ./... -race -count=1` 14/14 packages pass

## Discovered contracts (for future reference)
- **`Host.CollectMetrics` dispatch contract:** `OS="windows"` → base64-encoded powershell script via `host.wrapCommand`; `OS="linux"` → `/proc/stat`-based shell; `OS="darwin"` → `top -l 2` + `vm_stat` shell; anything else (e.g. `freebsd`) → falls back to the linux script. The test for the Windows dispatch asserts the wrapper surface (`powershell.exe` + `-EncodedCommand`) rather than the inner body, because `encodePowerShellScript` makes the body opaque. Body of `windowsMetricsScript()` is locked separately by `TestWindowsMetricsScript_Body`.
- **`agentic_fanout_test.go` snapshot contract:** both the assembled `containerAgenticFanoutCheckCommand` string and the spec map are snapshotted. Any unification refactor of #325 must update both snapshots in lockstep — the existing tests are the load-bearing coupling that keeps the two paths in sync today, not the code itself.
- **`scripts/benchstat` artifact retention:** GitHub-hosted artifacts default to 90 days. The bench-compare patch was attached to run `28545571496` on 2026-07-01, so the `agent-*` artifact is live until approximately late September 2026. Past that date, only the issue body or a re-implementation is available.
- **Mid-run reverts (re-confirmed):** no reverts observed this run. The metrics test file (a non-protected `_test.go`) survived commit + bridge call. The phantom-PR pattern from previous runs did not recur.

## Open-source posture
- Phantom-PR backlog now 16+ occurrences. Recovery mechanism now recommends the GitHub Actions artifact directly (`gh run download 28545571496 -n agent`) because `/tmp/gh-aw/` state is wiped between runs.
- All actions this run were well-formed (1 PR + 2 comments + 1 issue update). Safe to repeat.
- **Future test target** (if Task 9 is selected again): `internal/doctor/doctor.go` per-host check helpers — `checkContainerAgenticInnerHygiene`, `checkContainerRunnerInstall`, `hasContainerAgenticRunners`, `checkLinuxSudo`, `checkNative`, `checkOrphanRunners` have 0%–80% direct unit coverage. The existing `testutil.MockExecutor` + `host.NewHost`/`SetConn` infra is sufficient.
