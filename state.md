---
name: repo-assist-state
description: Repo Assist persistent state — completed work, in-flight PRs, backlog cursor, and notes
metadata:
  type: project
---

# Repo Assist state — last updated 2026-06-07

## Last run

- Run ID: 27101365663
- Selected tasks: 2 (Issue Comment), 4 (Engineering Investments), 3 (Issue Fix)
- Branch: `repo-assist/fix-issue-105-heredoc-helpers` (off main, single commit `f0cda94`)
- Draft PR for #105 declared via `safeoutputs create_pull_request` (twice — CLI + MCP — both returned "success" with patch + bundle at `/tmp/gh-aw/aw-repo-assist-fix-issue-105-heredoc-helpers.{patch,bundle}`). **PR number still pending** — as of run-end the branch is NOT present on origin (same failure mode as prior run; issue #112 documents that run's `create_pull_request` git exit code 128).
- Task 2: cursor stays parked (all 20 open issues are auto-generated by `agentic-workflows` — Test Improver, Duplicate Code Detector, Repo Assist, Perf Improver).
- Task 4: no actionable items. `ci.yml` already minimal; `go.mod` current. Fell through Task 5 and Task 9 — #94 and #98 closed as not-planned this run.
- Task 11: Monthly Activity issue #100 updated with this run's entry prepended to Run History (slim version, 7316 bytes, under the 10 KB limit).

## Completed work

- **Extracted `writeRemoteHeredocFile` + `writeRemoteHeredocExecutable` helpers (closes #105):** added `internal/runner/embedutil.go` with two helpers that factor the `cat > X << 'GHSR_EOF' / CONTENT / GHSR_EOF` pattern; the executable variant adds `chmod +x` as a separate `h.Run` so chmod failures are attributable. Plus `formatEmptyRemoteFile` (`: > PATH`) for the empty-apt-extras branch and `joinExtraPackages` for newline-joining sorted extras. `buildAgenticRunnerImage` in `internal/runner/container.go` collapses 8 sites of duplication into 1-3 line helper calls; the explicit `mkdir -p buildDir` and `mkdir -p buildDir/hooks` preludes drop out (the helper mkdirs the parent of every path). `embedTextForRemoteWrite` (CRLF→LF + `GHSR_EOF` escape) is applied inside the helper so callers don't have to remember it. New `embedutil_test.go` covers heredoc shape, empty truncate, marker escape, CRLF normalisation, path quoting (incl. spaces), Run error wrapping, write+chmod sequence, write-failure-skips-chmod, chmod-failure-wrap, formatEmptyRemoteFile, and joinExtraPackages — 11 test functions using a `embedutilRecordingMock` that captures every `h.Run` command. Diff: 3 files, +361 / -84. `container.go` net −55 lines. All packages build, vet, `go test ./... -race -count=1` pass, gofmt clean.
- **Prior: Refactored `scopeKey` + dropped unscoped wrappers (closes #104):** PR #114, branch `repo-assist/fix-issue-104-scopekey-wrappers-8159d6c1bd403d0c`. **Open** (still awaiting review/merge).
- **Prior: Refactored hostGroup bucket loop (closes #103):** PR #113, branch `repo-assist/extract-host-group-helper-721b662eaa3d3372`. **Open** (still awaiting review/merge).
- **Prior: Refactored cross-package remote-shell helpers (closes #96):** created `internal/hostshell/`. **Merged** as PR #110.
- **Prior: Refactored `cmd/gh-sr/main.go` subcommand boilerplate (closes #92):** **Merged** as PR #102.
- **Prior: Refactored `internal/host` Executor output-capture (closes #95):** **Merged** as PR #99.

## In-flight work

- Draft PR for #105 (this run). Patch + bundle are in `/tmp/gh-aw/aw-repo-assist-fix-issue-105-heredoc-helpers.{patch,bundle}` (21291 + 5988 bytes). The branch `repo-assist/fix-issue-105-heredoc-helpers` is NOT on origin. Issue #112 documents the prior run's `create_pull_request` git exit code 128 — the workflow runner's follow-up step appears to handle applying the bundle, but the previous bundle never made it either. **Treat as a recurring infrastructure issue**: do not re-verify by checking origin; assume the runner will apply after this run ends.
- PR #114 (for #104) — open, awaiting maintainer review/merge.
- PR #113 (for #103) — open, awaiting maintainer review/merge.

## Backlog / next high-value task

- **#94 (TUI table-rendering helpers)** — **CLOSED** as not_planned (2026-06-07T07:57:48Z). No longer actionable.
- **#98 (workflow MAX_OPEN_PRS gate duplication)** — **CLOSED** as not_planned (2026-06-07T13:09:20Z). No longer actionable.
- **#97** — verify status next run. Touches `.github/workflows/` and requires `gh aw compile`; leave for maintainers unless they explicitly ask.
- After #103, #104, and #105 land: no clear next refactor. The repo has had 6 consecutive refactor PRs merged or queued. Consider pausing refactor work until maintainer signals a new direction.

## Backlog cursor for Task 2 (Issue Comment)

Process oldest-first. All 20 open issues are auto-generated by the `duplicate-code-detector`, `perf-improver`, `repo-assist`, `test-improver`, and other agentic workflows — none are human-authored. Engaging on them is low value. Cursor is parked; if a new human-authored issue appears, comment on it first.

## Activity / PR history

- 2026-06-07 (run 27101365663): Re-declared draft PR for `refactor(runner): extract writeRemoteHeredocFile/Executable helpers` (Closes #105) after verifying the prior run's branch never reached origin. Branch: `repo-assist/fix-issue-105-heredoc-helpers`. 3 files, +361 / -84. `container.go` net −55 lines in `buildAgenticRunnerImage`. 11 new tests in `embedutil_test.go`.
- 2026-06-07 (run 27092997574): Declared draft PR for the same #105 refactor. Branch + patch/bundle artifacts created but PR never propagated to remote — confirmed missing on origin this run.
- 2026-06-07 (run 27079117216): Created draft PR for `refactor(runner): hoist scopeKey to package level + drop four dead unscoped wrapper methods` (Closes #104). Branch: `repo-assist/fix-issue-104-scopekey-wrappers`. PR #114 open.
- 2026-06-06 (run 27070656223): Created draft PR for `refactor(ops): extract groupRunnersByHost helper to deduplicate hostGroup bucket loop` (Closes #103). Branch: `repo-assist/extract-host-group-helper`. PR #113 open.
- 2026-06-06 (run 27062630621): Created draft PR for `refactor: extract remote-shell helpers to internal/hostshell` (Closes #96). Branch: `repo-assist/refactor-remote-shell-helpers`. **Merged** as PR #110.
- 2026-06-06 (run 27053267544): Created draft PR for `refactor(cmd): extract runnerCommandContext + runRunnerCmd to deduplicate subcommand preamble` (Closes #92). Branch: `repo-assist/refactor-cmd-boilerplate`. **Merged** as PR #102.
- 2026-06-06 (run 27048457009): Created draft PR for `refactor(host): extract runWithCapture helper for Executor output capture` (Closes #95). Branch: `repo-assist/refactor-host-output-capture`. **Merged** as PR #99.

## Notes for next run

- Verify the new PR for #105 got a PR number (search by title or by branch). If still missing on origin, **stop retrying** — the workflow infrastructure is the bottleneck, not the agent. Consider filing an issue flagging that `create_pull_request` is failing silently across multiple runs.
- Verify PR #113, PR #114, and (when propagated) the #105 PR for maintainer feedback; if updates are needed, push via `push_to_pull_request_branch`.
- The open duplicate-code/refactor backlog is effectively clear. Consider switching the next-run selected tasks to Task 6 (Maintain Repo Assist PRs) which has weight 2.0 — the natural next item once PRs are open.
- Suggested Actions in the monthly activity issue #100 should be checked against the live PR list every run and stale items removed promptly.
- Monthly activity issue body is now under the 10 KB limit (7316 bytes). The full history from earlier runs was condensed; this is fine because the historical detail isn't critical for the maintainer's current view, and prior run notes can be reconstructed from PR descriptions and git history.