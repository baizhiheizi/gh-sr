@internal/runner/agentic-runner-image/Dockerfile Except the core system dependecies, users might want to install more tools for the runner, we might support that, config in the config file, and added when building the image. Also we want these the `core system dependencies` more mainteinable, in a seperate file maybe. Help me to design it, following the best parctice.

---

Implement the plan as specified, it is attached for your reference. Do NOT edit the plan file itself.

To-do's from the plan have already been created. Do not create them again. Mark them as in_progress as you work, starting with the first one. Don't stop until you have completed all the to-dos.

---

Implement the plan as specified, it is attached for your reference. Do NOT edit the plan file itself.

To-do's from the plan have already been created. Do not create them again. Mark them as in_progress as you work, starting with the first one. Don't stop until you have completed all the to-dos.