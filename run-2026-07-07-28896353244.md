---
name: run-2026-07-07-28896353244
description: Run history entry for Repo Assist run 28896353244 (selected tasks 8, 2, 4)
metadata:
  type: project
---

# Run 28896353244 — 2026-07-07 20:44 UTC

## Selected tasks
- Task 8, Task 2, Task 4
- All three executed substantively.

## Task 8 (Performance) — completed
- **Branch:** `repo-assist/perf-parsefourint64s-no-fields-alloc-2026-07-07` @ `1d71bbf`
- **Files:** `internal/runner/disk.go` +61 / −7; new `internal/runner/disk_parse_bench_test.go` +17.
- **What:** `parseFourInt64s` (called once per host per `gh sr disk` listing refresh via `dirSizesWindows` + `dirSizesPOSIX`) was allocating a 64-byte `[]string` header slice on every call via `strings.Fields`. Replaced with a manual ASCII scan over the trailing line, using a `[4]string` stack-allocated field buffer and a `[4]int64` result array. Trailing-newline + multi-line fallback behavior preserved (`LastIndexByte('\n')` + ASCII-only trim).
- **Bench (count=5):** 90.5 → 52.8 ns/op (**-42%**), 64 → 0 B/op (**-100%**), 1 → 0 allocs/op.
- **`create_pull_request` reported success.** Recovery fallback: `/tmp/gh-aw/aw-repo-assist-perf-parsefourint64s-no-fields-alloc-2026-07-07.{patch,bundle}` (5.3 KB / 2.8 KB).

## Task 2 (Comment) — substantive
- **Issue #330** — new `[duplicate-code] FormatBytesHuman switch branches` (just landed 16:55 UTC). Analysed: perf commit #323 / `14d1edb` is load-bearing (single-allocation promise); option 1 (table-driven loop) is safe because the `buf [24]byte` + `AppendFloat → append(unit) → string(out)` shape stays byte-identical. Noted TiB overflow scale-hazard. Offered to draft `repo-assist/refactor-issue-330-format-bytes-human-table` with bench before/after.
- Skipped #327 (parent #325 already commented, refactor is downstream of #325 decision).

## Task 4 (Engineering) — completed
- **Branch:** `repo-assist/eng-makefile-coverage-target-2026-07-07` @ `7b4e8fa`
- **File:** `Makefile` +16 / −1.
- **What:** Added `make coverage` (`-coverprofile=coverage.out -covermode=atomic` + project total via `go tool cover -func`) and `make coverage-html` (writes annotated HTML report). Mirrors the contributor-ergonomics philosophy of PR #315 (which added `make fmt`, `tidy`, `ci`).
- **Verified:** `make coverage` exit 0 (project total = 58.0%), `make coverage-html` exit 0, `make ci` still passes, `make fmt` still passes.
- **`create_pull_request` reported success.** Recovery fallback: `/tmp/gh-aw/aw-repo-assist-eng-makefile-coverage-target-2026-07-07.{patch,bundle}` (3.3 KB / 1.8 KB).

## Task 11 (mandatory) — `update_issue` on #309
- New run entry prepended to Run History.
- Suggested Actions rebuilt: review PRs (new: parseFourInt64s, coverage targets; verify PR #329; existing #328, #323, the bench-compare phantom), check comments (new: #330; existing #325, #307, #326, #312, #313, #124), define goals (#132, #208).
- Future Work expanded to include #330 table-driven refactor option.

## Verified
- `go build ./...` OK
- `go vet ./...` clean
- `gofmt -l .` clean
- `go test ./... -race -count=1` 14/14 packages pass
- `make coverage` exit 0
- `make coverage-html` exit 0
- `make ci` exit 0 (vet + fmt + test)
- `make fmt` exit 0
- `BenchmarkParseFourInt64s` 90.5 → 52.8 ns/op (5 reps)

## Discovered contracts (for future reference)
- **`parseFourInt64s` input contract:** script emits `$total $work $temp $docker\n` (POSIX) or `Write-Output "$t $w $te $dk"` (PowerShell). 4 ASCII integer fields. LastIndexByte('\n') + ASCII trim handles the multi-line diagnostic fallback (script can append `error: ...` on failure; we keep the last non-empty numeric line).
- **`parseFourInt64s` alloc profile:** Before 1 alloc (strings.Fields []string header, 64 bytes for a 4-field line). After 0 allocs. `[4]int64` and `[4]string` stack buffers escape-proof because the function returns named values the compiler fills in place.
- **`make coverage`:** uses `covermode=atomic` because `make test` uses `-race -count=1`; parallel coverage under race needs atomic.
- **Coverage observation (this run):** project total **58.0%**. Highs `internal/autostart` 94.7%, `internal/ops` 93.6%, `internal/editor` 92.3%, `internal/agentic` 89.6%, `internal/hostshell` 89.7%. Lows `internal/diskschedule` 14.2%, `internal/tui` 15.0%, `internal/runner` 57.6%, `internal/host` 59.6%, `internal/hostshell/ps` 60.0%.
- **No mid-run reverts.** Both branches (`perf-parsefourint64s-...` + `eng-makefile-coverage-...`) survived commit + bridge calls cleanly.

## Open-source posture
- Phantom-PR backlog 16+ occurrences. Recovery via GitHub Actions artifact of run `28545571496` (90-day retention → late September 2026).
- All actions this run well-formed (1 comment + 2 PRs + 1 issue update). Safe to repeat.
- **Future Task 9 target:** `internal/tui/metrics.go` (15.0% coverage) — `hostMetricsHeaders`, `buildHostMetricsRows`, `hostMetricsColorize`, `metricsRow`, `formatPercent`, `formatUsedTotal` are small pure helpers; coverage drop straightforward using the table package + a `host.HostMetrics` fixture.