---
name: run-2026-07-15-29402596947
description: Run history entry for Repo Assist run 29402596947 (selected tasks 3, 2, 4)
metadata:
  type: project
---

# Run 29402596947 — 2026-07-15 09:43 UTC

## Selected: 3, 2, 4

- **Task 3 → Task 2 fallback:** live search found 0 open issues labelled `bug`, `help wanted`, or `good first issue`.
- **Task 2:** commented on #359 and #360 with the exact disposition through draft PR #363; commented on #369 and #370 with the SSH-round-trip-safe consolidation boundary. Advanced the issue cursor through #370.
- **Task 4 → Task 5 fallback:** read-only dependency/CI/runtime/build audit found no distinct low-risk engineering investment after excluding #373, open PRs, and recent Makefile/bench work. Implemented the low-risk #370 coding improvement instead.
- **Task 11:** fully replaced #309 using the required section ordering and checkbox/link format; included all 8 current open PRs, this run's temporary PR, all unacknowledged Repo Assist comments, #305 close action, and #373 decision.

## Implementation — `repo-assist/improve-linux-instance-probe-2026-07-15` @ `46ab07d`

Files: `internal/runner/linux_instance_probe.go` (new), `native.go`, `orphans.go`, `native_test.go`, `orphans_test.go`. Net +74/−70.

- Added `linuxInstanceProbe(h, instance, includeDir)` as the single owner of the one-call Linux S/U/Y/D shell probe and marker parser.
- Reduced `linuxSvcAndAutostartProbe` and `orphanLinuxPlanProbe` to thin wrappers while preserving their signatures and exactly one SSH round-trip.
- `includeDir=false` omits D for native lifecycle; `includeDir=true` includes D for orphan cleanup.
- Standardized parsing on `strings.TrimSpace`; added CRLF cases to both existing test tables.
- Corrected a latent orphan-path issue: the old `dir='$HOME/...'` assignment froze `$HOME` as literal text. The shared script leaves the sanitized path expandable and the test pins that command shape.
- Intentionally left `autostart.Detect` separate. Calling it from the composite runner probe would add an SSH round-trip; #369 would need pure shell-fragment/parser reuse to be worthwhile.

## Verification

- Focused: `go test ./internal/runner -run 'TestLinuxSvcAndAutostartProbe|TestOrphanLinuxPlanProbe' -count=1` — PASS.
- `go build ./...` — PASS.
- `go test ./... -count=1` — 17 packages PASS.
- `go test -race ./... -count=1` — PASS, no races.
- `go vet ./...` — clean.
- `gofmt` check — clean.
- `git diff --check` — clean.

## Safe outputs

- Comments accepted for #359, #360, #369, and #370.
- Monthly issue #309 update accepted.
- Draft PR prepared with temporary ID `#aw_probe370`; branch and commit are ready for `create_pull_request`.

## Follow-up

- Close #370 after the PR merges.
- Keep #369 open unless a no-execution helper design can deduplicate the third autostart probe without increasing SSH calls.
- Next Task 2 scan starts at #373, then resets to #132.
